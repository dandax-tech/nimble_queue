#include <iostream>
#include <new>

#include "atomic_bitset.hpp"


int main() {

    //nimble::queue<int, 1000> myqueue;
    /*
    nimble::atomic_bitset bset(1);
    bset.set(0, true);
    auto val = bset.get(0);

    std::cout << "value is " << val << std::endl;

    bset.set(1, true);*/
}
